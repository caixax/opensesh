//! Terminal sessions of the main window (Sprint 2): the Rust-owned session registry, and the pure
//! helpers and the gallery demo frame of the `TerminalItem` QML type (`bridge/terminal_view.rs`).

pub mod demo;
pub mod interaction;
pub mod preview;
pub mod profiles;
pub mod registry;
